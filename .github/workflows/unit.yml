name: unit
on: 
    pull_request:
        branches: 
            - main
jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        # - name: Setup Node.js
        - run: npm ci
        - run: npm run test
        # uses: actions/setup-node@v2
        # with:
        #     node-version: '16'